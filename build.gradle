// Root-level build.gradle (Project-Level)
allprojects {
    repositories {
        google()        // To fetch Android dependencies
        mavenCentral()  // For other dependencies like Kotlin
    }
}
