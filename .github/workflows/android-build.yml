name: Android CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # ✅ Install Gradle 8.0.2 on the runner
      - name: Set up Gradle 8.0.2
        uses: gradle/actions/setup-gradle@v3
        with:
          gradle-version: 8.0.2

      # ✅ Generate Gradle wrapper for 8.0.2 with SHA256
      - name: Generate Gradle Wrapper
        run: gradle wrapper --gradle-version 8.0.2 --gradle-distribution-sha256-sum ee15889a44dccf963c34a927c62880cb420f9f56cbb18c9af2e180248c1c0dd9

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Check Gradle Version
        run: ./gradlew --version

      - name: Build Debug APK
        run: ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: app-debug
          path: app/build/outputs/apk/debug/app-debug.apk
