pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
    plugins {
        id "com.android.application" version "8.0.2"
        id "com.android.library" version "8.0.2"
        id "org.jetbrains.kotlin.android" version "1.9.0" // only if using Kotlin
    }
}

rootProject.name = "HelloFintraq"
include(":app")
